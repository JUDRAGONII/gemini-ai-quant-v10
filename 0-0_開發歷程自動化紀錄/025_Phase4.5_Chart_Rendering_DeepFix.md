# 025_Phase4.5_Chart_Rendering_DeepFix (圖表渲染深度修復)

**文件編號**：DEV-LOG-006
**版本**：1.0.0
**建立日期**：2026-01-28
**目的**：修復 K線圖與技術指標面板雖然數據存在但畫面空白的渲染問題。

---

## 📅 自動產生日誌：2026-01-28

### 1. 工作項目：T-EMG-004 圖表渲染邏輯重構

**狀態**：✅ 已完成

#### 🛠️ 處理過程

- [x] **容器高度修復**：修正 `TechnicalIndicatorPanel.tsx` 中圖表容器 `div` 缺失 CSS 高度的問題，確保 `canvas` 能獲得正確的顯示空間。
- [x] **生命週期同步**：修正 `KLineChart.tsx` 將 `setData` 放在獨立於 `createChart` 的 `useEffect` 中所導致的 Ref 更新延遲問題。現改為「建立圖表即填充初始數據」。
- [x] **數據完整性強化**：在 `api/stocks/[symbol]/route.ts` 增加 `seenTimes` 去重邏輯，確保傳入圖表的時序數據唯一且嚴格升序，避免繪圖引擎因數據衝突而中斷渲染。
- [x] **抓取策略調整**：將 `useStockDetail` 的緩存數量從 200 提升至 300，確保 60MA 指標有足夠的計算深度。

#### 💡 技術決策

1. **顯式宣告空間**：Lightweight Charts 在初始化時需要穩定的容器尺寸，使用 `style={{ height }}` 確保在 React 渲染週期中空間被正確預留。
2. **原子化 Effect**：減少 Ref 作為依賴項的使用，改在副作用函數內部直接操作剛建立的實例，提高渲染成功率。

---

*文件編號：DEV-LOG-006*
*維護者：Antigravity Agent*
*最後更新日期：2026-01-28*
*修復狀態：✅ 已驗收圖表渲染邏輯*
