# 017_詳細測試案例集 (Detailed Test Cases)

**文件編號**：TEST-V10.0-001
**版本**：1.0.0
**建立日期**：2026-02-25
**功能說明**：本文件定義 AI 投資分析儀 V10.0 的核心測試案例，涵蓋單元測試 (Unit)、整合測試 (Integration) 與端對端測試 (E2E)。

---

## 1. 測試策略總覽

*   **測試框架**：Pytest (Backend), Vitest (Frontend)
*   **覆蓋率目標**：核心邏輯 (Service/ETL) > 90%
*   **測試資料**：使用 Mock (unittest.mock) 與 Fixtures 隔離外部 API。

---

## 2. 後端核心測試案例 (Backend)

### 2.1 宏觀數據 ETL (`etl/macro.py`)

| ID | 測試案例名稱 | 前置條件 | 測試步驟 | 預期結果 |
|:---|:---|:---|:---|:---|
| **TC-BE-001** | **FRED 數據同步 - 正常流** | Mock FRED API 回傳標準 JSON | 1. 呼叫 `sync_fred_data()`<br>2. 檢查 Supabase Upsert 呼叫 | 1. `pandas_datareader` 被正確呼叫<br>2. Supabase 收到正確格式的 `macro_indicators` 資料<br>3. 資料包含 `GDP`, `CPI`, `UNRATE` |
| **TC-BE-002** | **FRED 數據同步 - API 異常** | Mock FRED API 拋出 Timeout | 1. 呼叫 `sync_fred_data()` | 1. 捕捉 Exception<br>2. 程式不崩潰<br>3. Log 紀錄錯誤訊息 |
| **TC-BE-003** | **數據格式轉換** | Input: `date=2023-01-01, value=NaN` | 1. 傳入含 NaN 的 DataFrame | 1. NaN 資料被過濾，不寫入 DB |

### 2.2 AI 辯論引擎 (`agents/dialectic.py`)

| ID | 測試案例名稱 | 前置條件 | 測試步驟 | 預期結果 |
|:---|:---|:---|:---|:---|
| **TC-AI-001** | **多空辯論 - 完整流程** | Mock Gemini API 回傳 Bull/Bear/Judge 文字 | 1. 呼叫 `conduct_debate(code='2330', data={...})` | 1. 正方 Prompt 包含「成長型投資人」<br>2. 反方 Prompt 包含「價值型投資人」<br>3. 裁判 Prompt 包含雙方論點<br>4. 回傳 Dict 包含三方內容 |
| **TC-AI-002** | **裁判評分解析** | Mock Judge 回傳 "建議: Buy, 分數: 85" | 1. 呼叫辯論並解析裁判回應 | 1. 能從自然語言中正則提取 `85` 分與 `Buy` 建議 |

### 2.3 演化引擎 (`evolution_engine.py`)

| ID | 測試案例名稱 | 前置條件 | 測試步驟 | 預期結果 |
|:---|:---|:---|:---|:---|
| **TC-EV-001** | **個體初始化** | 無 | 1. 產生 10 個 Random Individual | 1. 每個個體長度為 26 (基因數)<br>2. 數值介於 0-1 之間 |
| **TC-EV-002** | **適應度評估 (Mock)** | Mock BacktestEngine 回傳 Sharpe=1.5 | 1. 呼叫 `evaluate_strategy(ind)` | 1. 回傳 tuple `(1.5, )` |
| **TC-EV-003** | **演化迭代** | 設定 `ngen=2` | 1. 執行 `run_evolution()` | 1. 完成 2 代演化無報錯<br>2. 回傳最佳個體 |

---

## 3. 前端測試案例 (Frontend)

### 3.1 儀表板 (`dashboard/page.tsx`)

| ID | 測試案例名稱 | 前置條件 | 測試步驟 | 預期結果 |
|:---|:---|:---|:---|:---|
| **TC-FE-001** | **報告列表渲染** | Mock Supabase `select` 回傳 3 筆報告 | 1. 渲染 Dashboard 元件 | 1. 畫面上顯示 3 張卡片<br>2. 股票代碼與摘要正確顯示 |
| **TC-FE-002** | **即時更新 (Realtime)** | Mock Supabase `on('INSERT')` 觸發 | 1. 模擬後端推送新報告 | 1. 列表最上方自動新增 1 筆資料<br>2. 不需重新整理頁面 |

---

## 4. 整合測試 (E2E)

### 4.1 全流程冒煙測試 (Smoke Test)

| ID | 測試案例名稱 | 測試步驟 | 預期結果 |
|:---|:---|:---|:---|
| **TC-E2E-001** | **每日排程流程** | 1. 觸發 Prefect `main_flow` | 1. 市場數據同步成功<br>2. 宏觀數據同步成功<br>3. 演化引擎產出新基因<br>4. Supabase 中 `evolution_genes` 表新增一筆紀錄 |

