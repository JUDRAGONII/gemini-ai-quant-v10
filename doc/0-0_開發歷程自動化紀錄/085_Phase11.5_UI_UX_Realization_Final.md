# Dev Log 085: Phase 11.5 UI/UX 實現與加固結案 (Final Hardening)

## 任務目標
完成數據監控中心與宏觀導航首頁的真實數據對接，移除所有 Mock 標籤，並實現玻璃擬態 (Glassmorphism) 設計與 RWD 適配。

## 執行細節
1. **後端修復**:
    - 重寫 `get_category_counts` RPC，補齊 `fx` 與 `economic_calendar` 統計邏輯。
    - 驗證後端 API `/api/v1/macro/calendar` 與 `/api/v1/macro/indicators` 連通性。
2. **前端重構**:
    - 實作 `useMonitorData` Hook，整合 SWR 5 秒自動輪詢。
    - 實作 `MonitorSkeleton` 提升加載體驗 (Perceived Performance)。
    - 重塑 `MonitorPage.tsx`:
        - 移除「待補」標籤。
        - 修正為 9 大分類（含匯率、貴金屬指標）。
        - 優化數據表格，處理 `null` 值顯示問題（改為 `-`）。
    - 重塑 `MacroPage.tsx`:
        - 整合 `EconomicCalendar` 組件。
        - 移除模擬數據標記。

## 變動檔案
- `backend/scripts/migrations/20260205_fix_category_counts_rpc.sql`
- `frontend/app/admin/monitor/page.tsx`
- `frontend/app/macro/page.tsx`
- `frontend/hooks/useMonitorData.ts`
- `frontend/components/macro/EconomicCalendar.tsx`

## 驗證結果
- [x] RPC 統計正常：fx (30k+), calendar (323)
- [x] SWR 自動重新驗證生效。
- [x] 骨架屏在刷新時正確顯示。

---
**核准狀態**：已完成 (Completed)
**時間戳記**：2026-02-05 15:20
**執行員**：AI 投資分析儀 (Antigravity)
