# 107 - Phase 14.7: 宏觀指數頁面雙語化與真實數據導入

**日期**：2026-03-02 ~ 2026-03-03
**階段**：Phase 14.7
**狀態**：✅ 已完成

---

## 📋 任務目標

將宏觀大數據導航頁面 (`app/macro`) 及其子明細頁 (`app/macro/[indicator]`) 實現全繁體中文化呈現，並將所有硬編碼假數據替換為 Supabase 真實連線。

## 🔧 變更清單

### 前端頁面
| 檔案 | 變更摘要 |
|:---|:---|
| `app/macro/page.tsx` | Quick Stats (GDP/CPI/FED) 從假數據改為 SWR 動態讀取；搜尋欄 placeholder、經濟日曆標題、指標陣列標題、Footer 徽章全面 `<Bilingual>` |
| `app/macro/[indicator]/page.tsx` | **完全移除 `@/data/mockMacro` 依賴**，改用 `useSWR` + `supabase.from('macro_indicators')` 取得即時歷史走勢；載入態、空態、導航列全面雙語化；「模擬數據警告」替換為「已連線至 SUPABASE 實體庫」標籤 |

### 前端子元件
| 檔案 | 變更摘要 |
|:---|:---|
| `MacroIndicatorCard.tsx` | `Last 24H` → `<Bilingual zh="近 24 小時" en="Last 24H" />` |
| `EconomicCalendar.tsx` | 錯誤態、空態、「實際:」標籤全面雙語化，新增 `Bilingual` import |
| `InsightsPanel.tsx` | 標題、副標題、分析說明、狀態標籤、引擎狀態全面 `<Bilingual>` |

### 測試
| 檔案 | 變更摘要 |
|:---|:---|
| `__tests__/app/macro/page.test.tsx` | Mock 從 Supabase 直接 Mock 改為 `jest.mock("swr")`；移除已廢棄的 `TC-7101 mockMacro` 測試；修正 `getAllByText` 以適配 Bilingual 多重匹配 |

## 📊 測試結果

```
Test Suites: 1 passed, 1 total
Tests:       10 passed, 10 total
Time:        7.676 s
```

## 💡 技術決策 (KISS)

- **不新增後端 API**：直接複用前端 `supabase-js` + `useSWR` 讀取 `macro_indicators` 表，省去中介層延遲。
- **靜態配置檔補全**：資料庫不含顏色/頻率/來源等 UI 層欄位，因此在前端建立 `INDICATOR_MAP` 常量映射，而非擴充 DB Schema。
