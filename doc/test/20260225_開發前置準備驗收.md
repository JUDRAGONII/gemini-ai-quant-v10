# 20260225_開發前置準備驗收 (Pre-development Readiness Test)

**文件編號**：TEST-PRE-001
**版本**：1.0.0
**建立日期**：2026-02-25
**測試目標**：驗證開發環境、基礎架構文件與核心依賴是否已準備就緒，符合「黃金參考實作 (Golden Reference)」標準。

---

## 1. 測試環境 (Test Environment)
- **OS**: Windows (User Local)
- **Frameworks**: Supabase (Docker), Python 3.x, Node.js (Next.js 14)
- **Reference Doc**: `AI 投資分析儀 V10.0 的完整可執行程式碼.md`

---

## 2. 測試案例清單 (Test Cases)

### 2.1 文件一致性驗證 (Documentation Integrity)

| ID | 測試案例名稱 | 基礎路徑 / 驗證點 | 預期結果 |
|:---|:---|:---|:---|
| **TC-PRE-001** | **黃金參考文件完整性** | 檢查 `AI 投資分析儀 V10.0 的完整可執行程式碼.md` 內容 | 1. 包含 `docker-compose.yml` (Sec 1.1)<br>2. 包含 `schema.sql` (Sec 2.1)<br>3. 包含前端 `dashboard/page.tsx` (Sec 4.4)<br>4. 包含 `agents/dialectic.py` (Sec 3.5) |
| **TC-PRE-002** | **架構文件映射** | 比對 `002_Docker...md` 與 Golden Reference | 1. 兩者定義的 Image 版本一致 (如 Postgres 15.1)<br>2. Port 定義一致 (8000/3000/5432) |
| **TC-PRE-003** | **Schema 文件映射** | 比對 `004_資料庫...md` 與 Golden Reference | 1. 資料表 `daily_price`, `macro_indicators` 欄位定義一致<br>2. 擴充套件 `vector` 宣告一致 |

### 2.2 環境配置驗證 (Environment Configuration)

| ID | 測試案例名稱 | 基礎路徑 / 驗證點 | 預期結果 |
|:---|:---|:---|:---|
| **TC-PRE-004** | **API Keys 安全性** | 檢查專案根目錄 `.env` (模擬檢查) | 1. 包含 `SUPABASE_KEY`<br>2. 包含 `GEMINI_API_KEY`<br>3. 包含 `TIINGO_API_KEY`<br>4. 檔案未被 commit (檢查 .gitignore) |
| **TC-PRE-005** | **Python 相依性** | 檢查 `requirements.txt` | 1. 包含 `prefect`, `supabase`, `deap`<br>2. 包含 `google-generativeai`, `langchain` |

### 2.3 安全性與邊界驗證 (Security & Edge Cases)

| ID | 測試案例名稱 | 邊界條件 / 攻擊向量 | 預期結果 |
|:---|:---|:---|:---|
| **TC-PRE-006** | **RLS 策略定義檢查** | 檢視 `schema.sql` 中的 RLS 區塊 | 1. `evolution_genes` 表啟用 RLS<br>2. 定義了 `auth.uid() = user_id` 的限制<br>3. 定義了 `service_role` 繞過權限 |
| **TC-PRE-007** | **Docker 網路隔離** | 檢視 `docker-compose.yml` 網路定義 | 1. 內部服務不暴露 Port 到 Host (除 5432, 8000, 3000)<br>2. `db` 密碼透過環境變數注入 |

---

## 3. 測試執行結果 (Execution Log)

- [ ] **TC-PRE-001**
- [ ] **TC-PRE-002**
- [ ] **TC-PRE-003**
- [ ] **TC-PRE-004**
- [ ] **TC-PRE-005**
- [ ] **TC-PRE-006**
- [ ] **TC-PRE-007**

---
**簽核**：(待測試完成後填寫)
