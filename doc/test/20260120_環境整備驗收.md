# 20260120_環境整備驗收計畫 (已執行)

本計畫旨在驗證 AI 投資分析儀 V10.0 的本地開發環境是否已正確配置。

## 測試案例清單

### 1. 基礎路徑驗證 (Basic Path)
- [x] **容器運行狀態**: 確保 `db`, `kong`, `auth`, `rest`, `ai-worker`, `ai-frontend` 皆處於 Running 狀態。
- [x] **API Gateway 連通性**: 訪問 `http://localhost:8000` (Kong) 應有正確回應。
- [x] **資料庫連線與 Schema**: 使用 `SERVICE_ROLE_KEY` 應能成功讀取 `daily_price` 表格 (Schema 42P01 已修復)。
- [x] **前端頁面加載**: 訪問 `http://localhost:3000` 應能看到 "Infrastructure Ready" 畫面 (Dockerfile Fixed)。

### 2. 邊界條件與一致性 (Boundary & Consistency)
- [x] **JWT 密鑰一致性**: 驗證 `.env` 中的 `JWT_SECRET` 是否與容器內配置一致（已執行 `fix_jwt.py` 排除 PGRST301 錯誤）。
- [x] **環境變數完整性**: 確保所有 `_API_KEY_1` 欄位皆已填值 (`test_env.py` Passed)。

### 3. Supabase RLS 安全性驗證 (Security)
- [x] **Supabase Admin**: 確認 `supabase_admin` 角色已建立，並啟用 `vector` 擴充套件。
- [ ] **匿名存取限制**: 使用 `ANON_KEY` 嘗試讀取 `evolution_genes` 應返回空值或受限（待後續應用層驗證）。
- [x] **Service Role 權限**: 使用 `SERVICE_ROLE_KEY` 應能完全存取所有表格。

---
> [!NOTE]
> 驗收結果：**通過**。環境已準備就緒，可進入開發階段。
