# Phase 11.5: UI/UX 實裝與系統加固 (UI/UX Realization & Hardening) 詳細計畫

## 1. 任務目標 (Executive Summary)
本子計畫旨在完成「數據監控中心」的最後一里路：**移除硬編碼的「待補」標籤**，並應用 `/ui-ux-pro-max` 規範將其轉化為真實、動態且極具金融質感的數據看版。同時，我們將加固前端的魯棒性，處理 API 異常時的退避與顯示。

## 2. 視覺與體驗設計 (UI/UX Pro Max)
採用 `/ui-ux-pro-max` 視角進行「Rich Aesthetics」升級：

### 2.1 [FIX] 數據解鎖與連動
*   **目標頁面**: `app/admin/monitor/page.tsx`
*   **變更**: 
    - 將「匯率」與「貴金屬」卡片的 `isMock` 旗標設為 false。
    - 串接後端 `get_category_counts()` 真實數據流。
    - 應用琥珀色 (Amber) 與 藍寶石色 (Cyan) 特色漸層。

### 2.2 [NEW] 骨架屏與微動畫 (Micro-animations)
*   **組件**: `MonitorSkeleton`
*   - 當數據載入時顯示具備「脈衝效果」的 Glassmorphism 骨架。

---

## 3. 執行路徑 (Execution Path)

### 第一階段：Mock 移除與數據綁定
1.  **[Frontend]** 修改 `MonitorPage` 元件，移除 hardcoded `status="待補"` 邏輯。
2.  **[Frontend]** 修正 `useMonitorData` Hook，確保能在數據真空時優雅回傳 0 筆而非崩潰。

### 第二階段：全站導航與一致性校準
1.  **[UI]** 檢查側邊攔 (Sidebar) 中的對應連結，確保 `Macro` 與 `Market` 的導航路徑精確。
2.  **[Refactor]** 統一全站 `StatusBadge` 樣式。

---

## 4. 測試與驗收標準 (TDD Master)

### 🧪 自動化測試案例 (Jest)
*   **TC-UI-01 (Red)**: 驗證「數據監控中心」頁面不再包含 "待補" 字樣。
*   **TC-UI-02 (Red)**: 模擬 API 延遲，驗證骨架屏是否正確觸發。
*   **TC-UI-03 (Red)**: 驗證 RWD 模式下 9 張卡片的折疊邏輯是否正常。

### ✅ 驗收 Checkbox
- [ ] 行政管理頁面 (Admin) 全部顯示為「實時數據」。
- [ ] 匯率卡片顯示確切的資料庫筆數。
- [ ] 頁面載入時間 (FCP) < 1.2s。

---

## 5. 安全性與生產硬化 (Hardening)
*   驗證 `/admin/monitor` 僅具備 `service_role` 或對應管理權限的用戶可見（若後續開啟 Auth）。
*   實作全局 `ErrorBoundary` 捕捉特定 API 500 錯誤。

---
**文件狀態**：待審核 (Pending Review)
**文件編號**：DOC-V10.3-045
