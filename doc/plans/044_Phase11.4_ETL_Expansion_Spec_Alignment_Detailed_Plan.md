# Phase 11.4: ETL 擴充與規格對齊 (ETL Expansion & Spec Alignment) 詳細計畫

## 1. 任務目標 (Executive Summary)
本子計畫旨在解決「物理審計 080」中 P1 級的功能缺口：**經濟日曆擷取器 (Economic Event Fetcher) 缺失**，並執行 **SDD (規格驅動開發)** 校準，確保代碼實作與《憲級文件》1:1 對齊。

## 2. 規格定義 (SDD Protocol)
採用 `/sdd` 視角進行合約鎖定：

### 2.1 [NEW] Economic Event Fetcher 規格
*   **功能**: 自動擷取 FRED 或相關來源之主要經濟事件（如非農、CPI 數據公布日）。
*   **目標表**: `public.economic_calendar`
*   **欄位**: `event_name`, `country`, `scheduled_at`, `importance`, `previous_value`, `consensus_value`.
*   **對應 API**: `/api/v1/macro/calendar`

### 2.2 [FIX] 指標代碼校準
*   同步 `MacroFetcher` 中的 `indicator_code` 與 `008_API 端點詳細規格.md` 之路徑。

---

## 3. 執行路徑 (Execution Path)

### 第一階段：ETL 功能實裝
1.  **[NEW] [Fetcher]** 實作 `backend/etl/economic_event_fetcher.py`。
    - 接入 FRED `releases` 系列 API。
2.  **[Refactor] [Fetcher]** 重構 `MacroFetcher` 處理 `GOLD` 與 `DXY` 的邊界案件。

### 第二階段：API 規格一致性修復
1.  **[API]** 校對 `backend/api/routers/market.py` 與 `screener.py` 中的回傳 JSON 鍵名 (Key names)。
2.  **[SQL]** 修復 `fn_screen_stocks` RPC 的錯誤捕捉邏輯 (Error handling inside PGSQL).

---

## 4. 測試與驗收標準 (TDD Master)

### 🧪 自動化測試案例 (Pytest)
*   **TC-ETL-01 (Red)**: 模擬 FRED API 回傳空值，驗證 Fetcher 穩定性。
*   **TC-ETL-02 (Red)**: 驗證 `economic_calendar` 資料寫入之排他性 (No duplicates)。
*   **TC-ETL-03 (Red)**: 調用 `/api/v1/macro/calendar` 驗證數據格式 (JSON Schema).

### ✅ 驗收 Checkbox
- [ ] `backend/etl/economic_event_fetcher.py` 可運轉。
- [ ] `public.economic_calendar` 填入未來 7 日之主要經濟事件。
- [ ] 所有 API 路由通過 `test_api_endpoints.py` 驗證。

---

## 5. 代碼審查標準 (Code Review)
*   **安全性**: API 端點必須通過 RLS 測試。
*   **效能**: ETL 寫入需使用 `batch_upsert` 減少資料庫連線壓力。
*   **可讀性**: 所有 Fetcher 必須包含繁體中文註釋。

---
**文件狀態**：待審核 (Pending Review)
**文件編號**：DOC-V10.3-044
