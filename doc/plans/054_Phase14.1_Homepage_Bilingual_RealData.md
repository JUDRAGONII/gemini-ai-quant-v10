# Phase 14.1: 首頁面板全繁體中文化與真實數據導入實作計畫

## 1. 當前專案狀況深度分析 (Current State Analysis)

### 1.1 前端現狀 (`app/page.tsx`)
- **總覽**: 已具備 Glassmorphism 基礎 UI 架構，能從 `macro_indicators` 與 `ai_reports` 拉取部分真實數據。
- **文案中英混雜**: 「市場導航儀」、系統狀態徽章、巨觀圖表標題皆為靜態硬編碼。
- **虛假數據**: 「系統效能中心」依賴寫死的靜態數據（Gemini 2.0 = Active、Latency = 120ms）。

### 1.2 後端支持度 (`api/v1/monitor/dashboard`)
- Phase 13.6 已建立聚合端點，提供系統資源 (CPU/RAM)、Redis 配額等監控數據。

### 1.3 測試覆蓋 (`__tests__/app/page.test.tsx`)
- 現有測試依賴硬編碼英文文字斷言，修改代碼需同步更新測試案例。

## 2. 方案設計與提案

### 2.1 頁面組件雙語化 (Bilingual Transformation)
- 全面導入 `<Bilingual>` 組件取代靜態文字。
- 圖表標題：`經濟成長 (GDP Growth QoQ)` / `消費者物價指數 (CPI)` / `恐慌指數 (VIX)`。
- `EnhancedReportCard` 根據 `report_type` 動態渲染雙語標籤。

### 2.2 系統效能真實數據掛載
- 新增 `HomeSystemHealth.tsx` Client Component。
- 使用 `useSWR` 定期拉取 `MonitorRepository.getDashboardSummary()`。
- 顯示真實 CPU/RAM/Gemini Token 配額，加入 Skeleton 骨架屏。

### 2.3 測試案例修復
- 全面改用 `getAllByText`，確保符合雙語渲染機制。

## 3. 執行結果
- **版本**: V10.5.4
- **測試**: Jest 53/53 PASS + TypeScript 0 errors
- **歸檔日期**: 2026-03-02
